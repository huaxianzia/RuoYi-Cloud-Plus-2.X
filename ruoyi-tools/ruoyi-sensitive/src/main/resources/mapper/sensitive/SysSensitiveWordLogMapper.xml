<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.sensitive.mapper.SysSensitiveWordLogMapper">

    <insert id="insert" parameterType="org.dromara.sensitive.domain.SysSensitiveWordLog">
        INSERT INTO sys_sensitive_word_log (
        id,
        trigger_field,
        sensitive_word,
        operator_id, <!-- 数据库字段是整数类型，与实体类Long匹配 -->
        operator_name,
        request_ip,
        request_url,
        status,
        create_time
        ) VALUES (
        #{id},
        #{triggerField},
        #{sensitiveWord},
        #{operatorId,javaType=java.lang.Long}, <!-- 显式指定类型（可选，MyBatis通常可自动识别） -->
        #{operatorName},
        #{requestIp},
        #{requestUrl},
        #{status},
        #{createTime}
        )
    </insert>

</mapper>
